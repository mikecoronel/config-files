server.port= 28080

##
# Datasource
##
spring:
  datasource:
    url: jdbc:mysql://localhost/issuetracker
    username: root
    password: password
    driverClassName: com.mysql.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    hibernate.ddl-auto: update
